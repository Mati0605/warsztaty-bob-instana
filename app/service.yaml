apiVersion: v1
kind: Service
metadata:
  name: workshop-app
  namespace: NAMESPACE  # <-- zamień na swój namespace, np. workshop-user01
  labels:
    app: workshop-app
spec:
  selector:
    app: workshop-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
